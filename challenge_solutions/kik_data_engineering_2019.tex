\documentclass{article}
\usepackage{color}
\usepackage{listings}

\begin{document}
\huge

\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\lstset{frame=tb,
  language=Python,
  aboveskip=3mm,
  belowskip=3mm,
  showstringspaces=false,
  columns=flexible,
  basicstyle={\small\ttfamily},
  numbers=none,
  numberstyle=\tiny\color{gray},
  keywordstyle=\color{blue},
  commentstyle=\color{dkgreen},
  stringstyle=\color{mauve},
  breaklines=true,
  breakatwhitespace=true,
  tabsize=3
}

\begin{lstlisting}
import numpy as np
mapping = np.zeros((10))
nsquare = 1
for n in range(1000):
    nsquare *= 2
    # converting to str is faster than successive divisions
    mapping[int(str(n)[0])] += 1

print(mapping)
\end{lstlisting}

\end{document}
